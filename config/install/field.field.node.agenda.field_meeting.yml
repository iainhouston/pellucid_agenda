langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_meeting
    - node.type.agenda
    - node.type.meeting
id: node.agenda.field_meeting
field_name: field_meeting
entity_type: node
bundle: agenda
label: Meeting
description: 'Use the autocomplete text box to find a Meeting that already exists on the wbsite'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:node'
  handler_settings:
    target_bundles:
      meeting: meeting
    sort:
      field: _none
field_type: entity_reference
